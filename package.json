{
  "name": "hive-log",
  "version": "0.1.0",
  "description": "Multi transport logging for hive",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bugs":"https://bitbucket.org/team-umbrella/hive-log/issues",
  "author": "Eric Satterwhite",
  "license": "MIT",
  "peerDependencies": {
    "hive-conf": "^0.1.0",
    "hive-stdlib": "^0.1.0"
  },
  "devDependencies": {
    "hive-conf": "^0.1.0",
    "hive-stdlib": "^0.1.0"
  },
  "bundledDependencies": [
    "winston",
    "hive-conf",
	"hive-stdlib"
  ],
  "dependencies": {
    "winston": "~0.8.0"
  },
  "optionalDependencies": {
    "winston-posix-syslog": "0.0.7"
  }
}
